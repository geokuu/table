import styled from "styled-components";

const AppStyle = styled.div`
  header {
    margin: 0 11px;
  }

  .logo {
    margin: 6px 0 4px 0;
  }

  .horizontal-line {
    height: 1px;
    width: 100%;
    background: var(--grey);
  }
`;

export default AppStyle;
